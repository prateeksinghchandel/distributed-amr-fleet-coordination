/**
 * robotColors.js — Shared robot color palette.
 *
 * Used by both the offline Simulation and the DistributedFleetState store so
 * that robot dots render with a consistent color scheme in every view.
 */
export const ROBOT_COLORS = ['#00c8ff', '#00d084', '#ffa94d', '#f06595', '#b197fc', '#ffd43b'];